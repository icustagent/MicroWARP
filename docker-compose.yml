services:
  microwarp:
    image: ghcr.io/ccbkkb/microwarp:latest
    container_name: microwarp
    restart: unless-stopped
    ports:
      - "${BIND_PORT}:${BIND_PORT}"
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - SYS_MODULE
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv4.ip_forward=1
    env_file:
      - .env
    volumes:
      - microwarp:/etc/wireguard
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 1G
    networks:
      - microwarp
      - apinetwork

volumes:
  microwarp:

networks:
  microwarp:
    name: microwarp
  apinetwork:
    name: apinetwork
